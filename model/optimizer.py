import pandas as pd

def generate_schedule(demand_df, employees_df):
    # placeholder schedule
    schedule_df = pd.DataFrame(columns=[
        "employee_id", "date", "shift", "assigned_hours"
    ])
    return schedule_df
